type Query {
    hello: String!
}

type Mutation {
    _empty: String
}

type PaginationPayload {
    totalDocs: Int!
    limit: Int!
    totalPages: Int!
    page: Int
    pagingCounter: Int!
    hasPrevPage: Boolean!
    hasNextPage: Boolean!
    prevPage: Int
    nextPage: Int
}

input PaginationInput {
    next: ID
    limit: Int
}
